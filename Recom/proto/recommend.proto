syntax = "proto3";
package RecommendServer;
option go_package = "./gen;recommend";

message request {
  string Token = 1;
}

message recommendation {
  repeated uint64 dish_ids = 1;
}

service RecommendService {
  rpc GetRecommendations (request) returns (recommendation);
}